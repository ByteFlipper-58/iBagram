package org.telegram.messenger.feature.chatmeta.domain.model

/**
 * Metadata types checked dynamically for visible chat messages.
 */
enum class MessageMetadataType {
    REACTIONS,
    EXTENDED_MEDIA,
    STORIES
}
