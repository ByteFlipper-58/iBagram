package org.telegram.messenger.feature.messaging.topics.domain.model

/**
 * Filter type for displaying forum topics.
 */
enum class TopicFilterType {
    ALL,
    OPEN,
    CLOSED,
    PINNED,
    HIDDEN
}
