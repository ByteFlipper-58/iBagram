package org.telegram.messenger.feature.messaging.stickers.domain.model

/**
 * Clean domain enum representing the type of sticker or sticker set.
 */
enum class StickerType {
    IMAGE,
    ANIMATED,
    VIDEO,
    EMOJI,
    MASK,
    UNKNOWN
}
